<h3>Make it Work! Contestant List:</h3>

  <% @contestants.each do |contestant| %>
    <div id="contestant-<%= contestant.id %>">
      <u><%= contestant.name %></u><br>
      Projects: <%= contestant.contestant_project_names.join(", ") %>
    </div>
    <br>
  <% end %>
